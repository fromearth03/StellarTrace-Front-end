# Server-side only (Vercel Function + local Vite middleware)
GEMINI_API_KEY=your_gemini_api_key_here

# Optional: override AI proxy URL in frontend if needed
# VITE_OPENROUTER_PROXY_URL=/api/openrouter
